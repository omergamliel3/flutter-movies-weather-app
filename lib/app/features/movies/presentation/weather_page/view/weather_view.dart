import 'package:flutter/material.dart';

class WeahterView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Weather view',
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}
